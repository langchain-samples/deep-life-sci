# Any shell script added here is run as bash, including under WSL2 and Git Bash on
# Windows, where a CRLF checkout breaks it at the shebang — `\r` becomes part of the
# interpreter name. There are none left, but the pin costs nothing and that failure is
# obscure enough to be worth pre-empting.
* text=auto
*.sh text eol=lf
